import BaseLayout from '@/components/layouts/base-layout';
import Hero from '@/features/home/components/hero';

const Home = () => {
  return (
    <BaseLayout>
      <Hero />
    </BaseLayout>
  );
};

export default Home;
